[pytest]
testpaths = tests
pythonpath = src tests scripts
addopts = -ra --strict-markers --strict-config
markers =
    integration: tests that exercise several components together
    e2e: browser tests against a deployed environment (Selenium, run by ci/e2e.sh)
filterwarnings =
    ignore::DeprecationWarning:seaborn

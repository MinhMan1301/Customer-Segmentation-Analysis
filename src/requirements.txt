# Direct runtime dependencies (pinned). The Docker image and CI install the
# fully resolved, hash-verified lock file generated from this list:
#   uv pip compile src/requirements.txt --universal --generate-hashes --python-version 3.12 -o src/requirements.lock
pandas==3.0.6
numpy==2.5.3
matplotlib==3.11.2
seaborn==0.13.2
streamlit==1.64.0
prometheus-client==0.26.0

# Staging: runs the committed sample data baked into the image (fast, no download).
APP_ENV=staging
APP_PORT=8502
DATA_DIR=/app/data/sample
MEM_LIMIT=1g
CPUS=1.0
# e2e: the sample has thousands of rows
E2E_MIN_DATASET_ROWS=1000
E2E_TIMEOUT=180
